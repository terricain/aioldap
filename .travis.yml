language: python

python:
    - 3.6

services:
    - docker

install:
    - pip install -Ur requirements-dev.txt
    - pip install .
    - pip install codecov

script:
    - make cov

after_success:
    - codecov